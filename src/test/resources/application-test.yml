spring:
  datasource:
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      max-lifetime: 1800000
      keepalive-time: 300000
  flyway:
    enabled: true
    locations: classpath:db/migration
  ai:
    openai:
      api-key: mock

telegram:
  bot:
    enabled: false
